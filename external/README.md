# external/

Vendored single-header libraries (added in later milestones):

- `stb_image.h`        — texture loading (engine side, M3)
- `FastNoiseLite.h`    — procedural noise (game side, M8)

These are dropped in here directly rather than pulled via vcpkg.
